import React from 'react';
//@ts-ignore
import styles from './CrmDealPopup.module.scss';

export const CrmDealPopup = () => {
    return (
        <div className={styles.container}>
            что должно быть. Частичная инфа по клиенту. Расширенная инфа по сделке (история, задачи,
            чаты)
        </div>
    );
};
